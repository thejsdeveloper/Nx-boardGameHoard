import { reviewDataAccessGames } from './review-data-access-games';

describe('reviewDataAccessGames', () => {
  it('should work', () => {
    expect(reviewDataAccessGames()).toEqual('review-data-access-games');
  });
});
