import { reviewUtilFormatter } from './review-util-formatter';

describe('reviewUtilFormatter', () => {
  it('should work', () => {
    expect(reviewUtilFormatter()).toEqual('review-util-formatter');
  });
});
